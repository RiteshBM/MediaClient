import 'package:flutter/material.dart';

class LoadingContainerListImage extends StatelessWidget {
	Widget build(context){
		return Container(
				color: Colors.grey[200],
				height: 220.0,
				width: 140.0,
			);
	}
}